const exerciseTimeMins = 40;

// let message = ''

// if (exerciseTimeMins < 30) {
//     message = 'You need to try harder!'
// }
// else {
//     message = 'Doing good!'
// }

const message =
  exerciseTimeMins < 30 ? "You need to try harder!" : "Doing good!"; //imagine you are asking a question to friend ? yes  : no

console.log(message);
